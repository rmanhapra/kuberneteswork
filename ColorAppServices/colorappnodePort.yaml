apiVersion: v1
kind: Service
metadata:
  name: colorapp-svc-np
  labels:
    app: colorapp-svc-np
spec:
  selector:
    app: colorapp
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30007
